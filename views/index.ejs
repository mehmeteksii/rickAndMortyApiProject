<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    
    <div class="container-fluid mt-4 px-4 py-5 bg-primary-subtle rounded">
        <div class="container-fluid row flex-lg-row-reverse align-items-center g-5 py-5">
          <div class="col-10 col-sm-8 col-lg-6">
            <img src="/images/resim.jpg" class="d-block mx-lg-auto img-fluid rounded" alt="Bootstrap Themes" width="700" height="500" loading="lazy">
          </div>
          <div class="col-lg-6">
            <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">Rick ve Morty Api Uygulaması</h1>
            <p class="lead">Rick ve Morty karakterlerini daha yakından tanıyın.</p>    
          </div>
        </div>
      </div>
    
      
      <div class="album py-5 bg-body-tertiary">
        <div class="container">
          <div class="row row-cols-1 row-cols-md-3 g-3">

            <% for (let i = 0; i <= (content.results.length)-1; i++) { %> 

                <!-- İkinci satır -->
                <div class="col">
                  <div class="card text-center">
                    <img src="<%= content.results[i].image %>" class="card-img-top" alt="<%= content.results[i].name %>">
                    <h3><%= content.results[i].name %></h3> 
                    <ul class="list-unstyled text-left">
                      <li><h5 class="d-inline">Status:</h5> <%= content.results[i].status %></li>
                      <li><h5 class="d-inline">Species:</h5> <%= content.results[i].species %></li>
                      <li><h5 class="d-inline">Gender:</h5> <%= content.results[i].gender %></li>
                    </ul>
                  </div>
                </div>
      
            <% } %>

          </div>
        </div>
      </div>
      
      
      
      <nav aria-label="...">
        <ul class="pagination justify-content-center">
            
            <% for (let i = 1; i <= content.info.pages; i++) { %> 
                <li class="page-item">
                    <a class="page-link" href="<%= getPageUrl(i) %>"><%= i %></a> 
                </li>
            <% } %>
            
        </ul>
    </nav>
      
    

      
      

</body>
</html>


